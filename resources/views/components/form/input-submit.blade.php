<div class="form-group row justify-content-start">
    <div class="col-md-{{$offset}}"></div>
    <div class="col-md-{{$col}}">
        <input id="{{$id}}" type="submit" value="{{$value}}" class="{{$class}}">
    </div>
</div>